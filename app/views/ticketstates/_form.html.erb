<%= form_for(@ticketstate) do |f| %>
  <% if @ticketstate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ticketstate.errors.count, "error") %> prohibited this ticketstate from being saved:</h2>

      <ul>
      <% @ticketstate.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :stateinfo %><br>
    <%= f.text_field :stateinfo %>
  </div>
  <div class="field">
    <%= f.label :start_time %><br>
    <%= f.datetime_select :start_time %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
